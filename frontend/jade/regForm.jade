//This template is virtualy generic to any text submission form that
//needs to be generated.
//Call with: JADE.regForm(formVals) where 'formVals' is a 'Forms' Collection
//JSON object containing 'Entry' models.
//IF REUSING, SUBMIT BUTTON TEXT MUST BE REPLACED
div
  form.pure-form(onsubmit="radio.trigger('submit form');")
    fieldset.pure-group
      - each item in locals
        input.pure-input-1-2(id='#{item.id}' name='#{item.name}' type='text' placeholder='#{item.desc}')
    button(type='submit' class=['pure-button pure-input-1-2 pure-button-primary']) Register