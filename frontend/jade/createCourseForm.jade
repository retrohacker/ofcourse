//This template is virtualy generic to any text submission form that
//needs to be generated.
//Call with: JADE.regForm(formVals) where 'formVals' is a 'Forms' Collection
//JSON object containing 'Entry' models.
//IF REUSING, SUBMIT BUTTON TEXT MUST BE REPLACED
div.ofcourse-form.ofcourse-centerImage
  !{JADE['xl-logo']()}
  form.pure-form
    fieldset.pure-group
      - each item in locals
        input.pure-input-1(id='#{item.id}' name='#{item.name}' type='text' placeholder='#{item.desc}')
    button(type='button' class=['pure-button pure-input-1 pure-button-primary'] onclick="radio.trigger('courseSubmit')") Create Course
